Usage: It is recommended to use the `bin/run.ps1` script to run `train_vrrl.py`.

```powershell
bin/run.ps1
```

This will load up the CARLA engine before starting the `train_vrrl.py` script.
Once the script terminates, it will ensure that the CARLA engine also terminates.